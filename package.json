{
  "name": "p2convert",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "backend": "cd backend && npm start",
    "backend:dev": "cd backend && npm run dev",
    "backend:worker": "cd backend && npm run worker",
    "backend:pm2": "cd backend && npm run pm2:start",
    "deploy": "cd backend && ./scripts/deploy.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^5.1.0",
    "multer": "^2.0.2"
  }
}
